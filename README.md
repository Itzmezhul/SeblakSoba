
  # Design website

  This is a code bundle for Design website. The original project is available at https://www.figma.com/design/yAG50H0gVv6SOImwK3yrgR/Design-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  